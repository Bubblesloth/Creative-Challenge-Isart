/// @description Insérez la description ici
// Vous pouvez écrire votre code dans cet éditeur

if (global.leaubrun_conv && !global.indecis_quest && destroy) || (global.leaubrun_completed && destroy){
    instance_destroy()
}

if replaceFlag == false{
    scr_replace(self,1);
    replaceFlag = true;
}

if part x+=10