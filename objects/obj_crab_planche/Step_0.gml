/// @description Insérez la description ici
// Vous pouvez écrire votre code dans cet éditeur
if replaceFlag == false{
    scr_replace(self,1);
    replaceFlag = true;
}

if global.destroy_plank instance_destroy()